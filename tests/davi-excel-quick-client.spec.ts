import {expect,test} from '@playwright/test'

const fixture=`<!doctype html><html><head><link rel="stylesheet" href="/src/styles/rebrand-v2.css"><link rel="stylesheet" href="/src/components/ui/Modal.css"><link rel="stylesheet" href="/src/components/DaviQuickClientModal.css"></head><body><div class="ui-modal-layer"><button class="ui-modal-scrim" aria-label="Fechar"></button><div class="ui-modal-panel ui-modal-panel--md" role="dialog" aria-modal="true"><div class="ui-modal-title"><div><span>DAVI EXCEL</span><h2>NOVA CLIENTE</h2></div></div><div class="ui-modal-body"><form class="davi-quick-client"><p>Cadastre a cliente sem sair da planilha. A venda continuará como rascunho.</p><label><span>NOME *</span><input value="ILDE MAE"></label><div><label><span>TELEFONE</span><input value="(11) 99999-9999"></label><label><span>CPF</span><input value="000.000.000-00"></label></div><label><span>E-MAIL</span><input value="ilde@example.com"></label><section class="davi-client-duplicates"><header><div><strong>Encontramos clientes parecidas.</strong><span>Confira antes de criar uma duplicata.</span></div></header><div><span><strong>ILDE MARIA</strong><small>Nome parecido</small></span><button>USAR CLIENTE EXISTENTE</button></div><button>CADASTRAR MESMO ASSIM</button></section></form></div><div class="ui-modal-footer"><button>CANCELAR</button><button class="primary">SALVAR CLIENTE</button></div></div></div></body></html>`

for(const width of [390,1440])test(`cadastro rápido permanece compacto e utilizável em ${width}px`,async({page})=>{await page.setViewportSize({width,height:844});await page.route('**/fixture',route=>route.fulfill({contentType:'text/html',body:fixture}));await page.goto('/fixture');const modal=page.getByRole('dialog');await expect(modal).toBeVisible();await expect(page.locator('input[value="ILDE MAE"]')).toBeVisible();await expect(page.getByRole('button',{name:'SALVAR CLIENTE'})).toBeVisible();await expect(page.getByRole('button',{name:'USAR CLIENTE EXISTENTE'})).toBeVisible();expect(await modal.evaluate(node=>node.getBoundingClientRect().width)).toBeLessThanOrEqual(Math.min(560,width));expect(await page.evaluate(()=>document.documentElement.scrollWidth)).toBeLessThanOrEqual(width)})
